// Write a function matcher in python that takes a string containing braces ([{()}]) as a parameter and returns True if the braces are matched, 
// and False otherwise. The braces may be nested. Your solution should not use any built-in or library functions other than those in the Stack and Queue classes provided.

const Stack = require('./Stack');
// const Queue = require('./Queue');


const matcher = (str) => {


}



const main = () => {
  console.log(matcher("[()]") === true);
}
main();


module.exports = matcher;